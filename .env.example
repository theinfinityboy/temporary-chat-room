# Copy to .env to enable real multi-user sync locally:
# VITE_WS_URL=ws://localhost:8080
#
# Without VITE_WS_URL the app uses the in-memory mock transport (per-tab only).
# With it, run the relay first: npm run server
# Server port can be overridden: PORT=8080 node server/index.js
